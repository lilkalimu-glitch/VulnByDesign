version: "3.9"

services:
  vulnbydesign:
    build: .
    container_name: vulnbydesign
    ports:
      - "5000:5000"
    volumes:
      - ./db:/app/db
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    restart: unless-stopped
